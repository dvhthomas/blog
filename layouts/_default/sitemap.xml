{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Data.Pages }}
  {{- if ne .Kind "taxonomy" -}}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}
    {{- $priority := .Sitemap.Priority -}}
    {{- if eq .Kind "home" -}}
      {{- $priority = 1.0 -}}
    {{- else if eq .Kind "page" -}}
      {{- if or (eq .Section "about") (eq .RelPermalink "/about/") -}}
        {{- $priority = 0.9 -}}
      {{- else if or (eq .Section "resume") (eq .RelPermalink "/resume/") -}}
        {{- $priority = 0.9 -}}
      {{- else if not $priority -}}
        {{- $priority = 0.5 -}}
      {{- end -}}
    {{- else if eq .Kind "section" -}}
      {{- if or (eq .Section "blog") (eq .Section "til") -}}
        {{- $priority = 0.9 -}}
      {{- else if not $priority -}}
        {{- $priority = 0.8 -}}
      {{- end -}}
    {{- else if eq .Kind "term" -}}
      {{- $priority = 0.3 -}}
    {{- else if not $priority -}}
      {{- $priority = 0.5 -}}
    {{- end -}}
    {{ if ge $priority 0.0 }}
    <priority>{{ $priority }}</priority>{{ end }}
  </url>
  {{- end -}}
  {{ end }}
</urlset>
