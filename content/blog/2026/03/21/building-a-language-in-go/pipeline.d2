direction: right

classes: {
    spec: {
        style.fill: "#E8F4FD"
    }
    impl: {
        style.fill: "#FFF3E0"
    }
}

Source Text -> Lexer -> Parser -> Semantic -> Interpreter -> Result

Lexer: { class: spec }
Parser: { class: spec }
Semantic: { class: spec }
Interpreter: { class: impl }
